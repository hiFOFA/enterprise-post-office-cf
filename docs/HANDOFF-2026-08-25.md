# 企业邮局项目交接文档（2026-08-25）

> **用途**：供接替开发的 AI / 人类快速接管。内容来自本仓库文件 + 本次 Cursor 会话已核实记录；**未在仓库或会话中确认的事项会标明「待核实」**。  
> **原始会话索引**：`/Users/hihi/.cursor/projects/Users-hihi-Desktop-cloudflare-temp-email-main-3/agent-transcripts/33d7e946-3d6a-41cf-9012-3e06d85925c3/33d7e946-3d6a-41cf-9012-3e06d85925c3.jsonl`

---

## 1. 项目是什么

| 项 | 内容 |
|---|---|
| 本地路径 | `/Users/hihi/Desktop/cloudflare_temp_email-main-3` |
| 上游基础 | [dreamhunter2333/cloudflare_temp_email](https://github.com/dreamhunter2333/cloudflare_temp_email) |
| 产品定位 | **企业邮局**（非临时邮箱）：三级身份、管理端开号、每邮箱有密码、可收可发 |
| 技术栈 | Cloudflare Workers + D1 + Email Routing + Vue 3 前端（打进 Worker `[assets]`） |
| 对外版本号 | **v1.0**（`frontend/package.json`、`worker/package.json`） |
| D1 `DB_VERSION` | **v0.0.13** — 用户明确要求**不要改** |
| GitHub 仓库 | https://github.com/hiFOFA/enterprise-post-office-cf |
| GitHub 用户名 | **hiFOFA**（由 `888888888881` 改名） |
| README 标题 | `# 企业邮局-**Cloudflare**` |
| README 副标题 | 不用自备服务器。这是一个运行在 Cloudflare 生态上的完整、稳定的企业邮局。 |
| 页脚鸣谢 | 仅保留 upstream `cloudflare_temp_email` 链接，不推广其他旧仓库 |

---

## 2. 产品铁律（不可回退）

1. **公开 `POST /api/new_address` 永远 403** — 不允许为方便打开匿名开号。
2. **三级身份**：主管理 / 子管理 / 邮箱用户；开号只走管理端。
3. **每个邮箱在本系统有密码**，是完整门户，不是一次性临时地址。
4. **令牌 + 令牌页 API 文档** 可交给 AI 做自动化；权限与网页操作同级。文档路径：
   - `frontend/public/api-docs/mailbox-user.md`
   - `frontend/public/api-docs/main-admin.md`
   - `frontend/public/api-docs/sub-admin.md`
   - 线上同源：`/api-docs/*.md`
5. **部署技能**：`skills/deploy-cf-post-office/SKILL.md` + `reference.md`；Cursor 指针 `.cursor/skills/deploy-cf-post-office/SKILL.md`（`.cursor/` 在 gitignore，不进 GitHub）。
6. **密钥不进 git**：`worker/wrangler.toml`、`.env*`、API 令牌、管理员密码等。
7. **README 亮点文案**（用户定稿）：「**对自动化协议以及 AI 开发兼容很好**」— 强调令牌页文档丢给 AI 即可自动化，不是「AI 帮你部署」那条。
8. **代码库内已移除** `mail.weiwu.bond` 等演示站链接（2026-08-25 检索 `weiwu.bond` 无匹配）。

---

## 3. 品牌 / Logo

| 文件 | 说明 |
|---|---|
| `docs/brand/logo.png` |  canonical logo（金信封，透明底，约 1024²） |
| `frontend/public/logo.png` | 线上静态资源同源 |
| `frontend/public/favicon.ico` | favicon |
| `Pasted Graphic.png` | gitignore，仅本地素材 |
| `1.jpg` | gitignore，用户曾用作 GitHub 头像素材 |

- README 中 logo：`<img src="docs/brand/logo.png" width="100" ...>`（中英文一致）。
- 登录页 `frontend/src/views/common/EnterpriseLogin.vue` 使用 `/logo.png`。
- 页头 `frontend/src/views/Header.vue` 使用 `.header-logo` + `/logo.png`。

**2026-08-24/25 已做**：构建 `frontend` 后 `wrangler deploy` 更新线上 logo；部署后验证 `mail.weiwu.bond/logo.png` 与 `mail.bingd.cyou/logo.png` 字节与本地一致（1326265 bytes，sha256 前缀 `f7bb3eeb962a`）。**仅更新静态资源，未动 D1 数据。**

---

## 4. Cloudflare 生产环境（来自本地 `worker/wrangler.toml`，该文件 **gitignore**）

| 项 | 值 |
|---|---|
| Cloudflare Account ID | `e061df57922fcdeaa5617a8100cf3065` |
| Worker 名称 | `dry-sun-1257` |
| D1 库名 | `temp-email` |
| D1 database_id | `91393dbb-98ae-46c7-8a34-ad56dd43b826` |
| KV binding | `KV`，id `14041d46598f408082d4e5095ccfcff7` |
| `workers_dev` | `false` |
| `[assets]` | `../frontend/dist/`，`run_worker_first = true` |
| `PREFIX` | `""` |
| `keep_vars` | `true`（线上 `ADMIN_USERNAME` 等由仪表盘保留，toml 里不要写死 admin） |

### 4.1 访问域名（仪表盘已绑，**不要**在 wrangler.toml 里 resync routes，易 403）

- `mail.weiwu.bond`
- `mail.bingd.cyou`

注释原文：`# Do not resync routes on deploy — zone updates often 403.`

### 4.2 `DOMAINS` / `DEFAULT_DOMAINS`（当前 wrangler.toml）

```
1cccc.cyou, 886454.cyou, bingd.cyou, tanxiang.bond, zhaoxi.bond, flyingcow.skin, sbynb.top
```

已 `wrangler deploy` 同步到 Worker 环境变量（会话记录：2026-08-25）。

### 4.3 Email Routing（2026-08-25 会话中通过 Cloudflare API 配置）

两域均在**同一 CF 账号**，zone 状态 `active`：

| 域名 | zone_id | Email Routing | Catch-all |
|---|---|---|---|
| `flyingcow.skin` | `ba1f18ed04f9312657a244fe4bae5bbf` | enabled, status `ready` | → Worker `dry-sun-1257` |
| `sbynb.top` | `ddc25d7a445b2736ac985b4cdf3e8e96` | enabled, status `ready` | → Worker `dry-sun-1257` |

**DNS 处理原则（用户要求）**：

- `flyingcow.skin`：原无记录；仅加 MX×3 + SPF TXT + DKIM TXT。
- `sbynb.top`：**保留** `sbynb.top`、`api.sbynb.top`、`ws.sbynb.top`、`test.sbynb.top` 的 A/AAAA；**删除**冲突的旧 MX/SPF/DKIM 后由 Email Routing API 重新写入正确收信记录。

**待用户验证**：两域开测试邮箱后，外网发信是否进箱（会话末未记录端到端收信测试结果）。

### 4.4 朋友贡献域名（用户问过，已解释）

- **不行**：朋友在自己 CF 账号只加一条 DNS、不托管到你账号 → Catch-all **无法**指向你账号的 `dry-sun-1257`。
- **可行**：zone/子域 NS 委派到你账号，或加你为 zone 管理员且 Worker 在同账号；README 亦写明 DNS 区与 Worker 须同一账号。

---

## 5. GitHub 仓库状态

| 项 | 内容 |
|---|---|
| URL | https://github.com/hiFOFA/enterprise-post-office-cf |
| 默认分支 | `main` |
| 维护者链接 | README 中 `hiFOFA` |
| 仓库 Description | `运行在 Cloudflare 生态上的完整、稳定的企业邮局。` |
| Homepage | 已清空（不再挂演示站） |
| Topics | cloudflare, cloudflare-workers, cloudflare-d1, email-routing, enterprise-email, webmail, vue, wrangler 等（会话中通过 API 设置） |
| `PROJECT_GITHUB_URL` | `frontend/src/utils/projectLinks.js` → hiFOFA 仓库 |

### 5.1 Contributors / 提交作者（用户强烈在意）

- **Cursor `cursoragent` 共创已清除**：历史提交曾自动带 `Co-authored-by: Cursor <cursoragent@cursor.com>`，已 filter-branch 去掉。
- **用户要求**：代码行数全算 **hiFOFA**；Claude 仅「共创」、**0 代码量**。
- **最终做法（会话记录）**：
  - 重写提交作者为 `hiFOFA <bingd202312@gmail.com>`；
  - 首包大提交**不含** Claude trailer；
  - 另有一次 **0 行 diff** 的空提交带 `Co-authored-by: Claude Code <noreply@anthropic.com>`；
  - README 维护者行文字致谢 Claude Code（非 GitHub Co-authored 行数统计）。
- **待核实**：GitHub Contributors 页面统计可能有缓存延迟；接替者应刷新 graphs/contributors 确认。

### 5.2 已知 Git 提交主题（会话记录，SHA 因 force-push 可能已变）

按时间大致顺序：

1. Publish the enterprise post office on GitHub.
2. Shrink the README mark and drop the public demo URL.
3. Clarify AI automation in the README and enlarge the mark.
4. Point clone URLs at the renamed GitHub account.
5. Halve the README mark size.
6. Credit Claude Code as co-creator in the README. / Add Claude Code as co-creator.（空提交）

**注意**：`worker/wrangler.toml` **从未 commit**；域名列表变更仅存在于本地 toml + 线上 Worker。

---

## 6. 密钥与安全（重要）

用户在聊天中**多次粘贴**以下敏感信息（**本文档不重复完整令牌**）：

- GitHub PAT（`github_pat_…`，多枚）
- Cloudflare API Token（`cfat_…`，含「管理域名」「全权限无域名」两枚）

**接替者必须**：

1. 假定这些令牌已泄露 → **立即在 GitHub / Cloudflare 仪表盘撤销**，再发新令牌。
2. 永远不要把令牌写入 git、README、wrangler.toml 或交接文档。
3. 部署用 `export CLOUDFLARE_API_TOKEN=…` + `wrangler whoami` 验证后再 deploy。
4. 部署完成后提醒用户撤销本次会话令牌（部署技能铁律）。

本地 git 提交者曾显示 `hi hi <hihi@hideMacBook-Pro.local>` — 与 GitHub `hiFOFA` 未绑定，已通过历史重写修正远程作者。

---

## 7. 部署与更新流程（仅改前端/logo/vars 时）

```bash
cd /Users/hihi/Desktop/cloudflare_temp_email-main-3

# 1. 前端构建（logo 在 public/ → dist/）
cd frontend && pnpm i && pnpm build

# 2. 部署 Worker（需 CLOUDFLARE_API_TOKEN + ACCOUNT_ID）
cd ../worker
export CLOUDFLARE_ACCOUNT_ID='e061df57922fcdeaa5617a8100cf3065'
export CLOUDFLARE_API_TOKEN='<用户新发的令牌>'
npx wrangler whoami   # 必须通过
npx wrangler deploy   # 不要擅自改 routes
```

**验收**（部署技能要求）：

- `GET /health_check` → 200
- `GET /open_api/bootstrap` → 200，含站点 title
- `GET /open_api/settings` 未登录 → **401**
- `/` → 登录页 HTML，含 `logo.png`

---

## 8. 代码结构速查

```
worker/                 # Hono Worker，管理 API + 收信
  wrangler.toml         # 本地 only（gitignore）
  wrangler.toml.template
  src/worker.ts
frontend/               # Vue 3
  public/logo.png
  src/views/common/EnterpriseLogin.vue
db/schema.sql           # D1 初始化
skills/deploy-cf-post-office/
docs/brand/logo.png
```

**关键行为**（会话中已实现，具体 diff 在 git 历史）：

- 公开 bootstrap / settings 鉴权策略（有测试：`worker/test/`、`frontend/src/utils/__tests__/`）
- 登录前 bootstrap、登录后 settings
- `POST /api/new_address` → 403

---

## 9. 用户偏好与未完成项

### 9.1 已明确用户偏好

- 不要自动给 Cursor 加 GitHub 共创。
- README logo 大小：当前 **width=100**（曾 40→200→100）。
- 侧边栏/文档**不要**挂 `mail.weiwu.bond` 以免引流压垮演示站。
- 域名 DNS：**尽量保留**非冲突记录；收信冲突的 MX/SPF/DKIM 可删后由 Email Routing 重写。
- 只更新 logo 时：**不要**重建 D1、不要删已有邮箱数据。

### 9.2 待核实 / 未在会话中完成

| 项 | 状态 |
|---|---|
| `flyingcow.skin` / `sbynb.top` 实际收信测试 | 用户未反馈结果 |
| GitHub 头像是否已改为 `1.jpg` | 用户自行在 settings 操作，会话未验证 |
| 本地 `git status` 是否有未提交 README/wrangler 变更 | 本会话写交接文档时 shell 未返回，**接替者请本地执行 `git status`** |
| 两枚 CF 令牌是否已撤销 | 未知，应视为未撤销直至用户确认 |
| 用户曾提「删仓库重建同名仓库」 | 后续实际走的是 **重写 git 历史** + Contributor 修正，**未**删除 GitHub 仓库 |

---

## 10. 给接替 AI 的第一条指令（可直接粘贴）

```
你正在接管 Cloudflare 企业邮局项目。
仓库：https://github.com/hiFOFA/enterprise-post-office-cf
本地：/Users/hihi/Desktop/cloudflare_temp_email-main-3
请先阅读 docs/HANDOFF-2026-08-25.md 和 skills/deploy-cf-post-office/SKILL.md。
生产 Worker：dry-sun-1257，Account e061df57922fcdeaa5617a8100cf3065。
不要改 D1 DB_VERSION；不要打开 POST /api/new_address；不要把密钥写入 git。
任何 Cloudflare 操作前先 wrangler whoami；令牌只放环境变量。
```

---

## 11. 文档生成说明

- 生成时间：2026-08-25（UTC+8 会话）
- 生成方式：基于仓库可读文件 + 本会话已执行操作记录汇编
- **未编造**：未写入未经会话或文件证实的 CF 密钥全文、未证实的 git SHA、未完成的测试结果
